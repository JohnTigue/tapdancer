service: chucker

plugins:
  - serverless-offline
  
provider:
  name: aws
  region: us-east-1
  runtime: nodejs6.10
  stage: dev

# you can add statements to the Lambda function's IAM Role here
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "s3:PutObject"
        - "s3:GetObject"
        - "s3:PutObjectAcl"
      Resource: "arn:aws:s3:::chuckstaplist.com/*"
#    - Effect: "Allow"
#      Action:
#        - "s3:PutObject"
#      Resource:
#        Fn::Join:
#          - ""
#          - - "arn:aws:s3:::"
#            - "Ref" : "ServerlessDeploymentBucket"
#            - "/*"

# you can define service wide environment variables here
#  environment:
#    variable1: value1

# you can add packaging information here
#package:
#  include:
#    - include-me.js
#    - include-me-dir/**
#  exclude:
#    - exclude-me.js
#    - exclude-me-dir/**

functions:
  updater:
    handler: src/lambdas/chucksProcessor/ChucksProcessor.freshenMenu
    events:
      - schedule: rate(1 minute)

  # TODO: Kill these 3      
  # This was made for testing the theory that Lamda cannot overwrite root's files.
  # I.e. why is getObject working but putObject gets AccessDenied
  seeder:
    handler: seed_beers_dot_json.seedBeersDotJson
    # no events just invoked manual (once hopefully) for AWS Console Web UI
    
  hello:
    handler: handler.hello

  # TODO: Kill this off since not FTPing anymore
  ftp-tester:
    handler: src/lambdas/ftp_tester/ftp_tester.testFtp
    
#      - http:
#          path: users/create
#          method: get
#      - s3: ${env:BUCKET}
#      - sns: greeter-topic
#      - stream: arn:aws:dynamodb:region:XXXXXX:table/foo/stream/1970-01-01T00:00:00.000
#      - alexaSkill
#      - iot:
#          sql: "SELECT * FROM 'some_topic'"
#      - cloudwatchEvent:
#          event:
#            source:
#              - "aws.ec2"
#            detail-type:
#              - "EC2 Instance State-change Notification"
#            detail:
#              state:
#                - pending

#    Define function environment variables here
#    environment:
#      variable2: value2

# you can add CloudFormation resource templates here
#resources:
#  Resources:
#    NewResource:
#      Type: AWS::S3::Bucket
#      Properties:
#        BucketName: my-new-bucket
#  Outputs:
#     NewOutput:
#       Description: "Description for the output"
#       Value: "Some output value"
